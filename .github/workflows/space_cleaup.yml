- name: Free up disk space
  run: |
    df -h
    sudo rm -rf /usr/local/lib/android /opt/google /opt/hostedtoolcache /usr/share/dotnet
    sudo apt-get autoremove -y
    sudo apt-get clean
    docker system prune -a -f
    df -h  # 显示清理后剩余空间
